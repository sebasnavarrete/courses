// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyD2k8qZ52KgTmPTKeNCcxRM73XOjRUfo2o',
    authDomain: 'notas-app-sn.firebaseapp.com',
    databaseURL: 'https://notas-app-sn.firebaseio.com',
    projectId: 'notas-app-sn',
    storageBucket: '',
    messagingSenderId: '736269116186',
    appId: '1:736269116186:web:9afb0e02725e0be6'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
